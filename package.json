{
  "name": "groupnetwork",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test:api": "node --test api/test/invariants.test.js api/test/router.test.js",
    "validate:api": "sam validate --lint --template api/template.yaml"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "@types/three": "^0.177.0",
    "three": "^0.177.0"
  }
}
